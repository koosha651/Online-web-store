<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Piano Shop</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@600&display=swap" rel="stylesheet">
</head>

<body>
    <div class="header">
        <a href="/"> <img class="header" src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ33SSWrvfsbOUqSM3KoJq1E-B4oiqQA1U6Cg&usqp=CAU"
                alt=""></a>
        <a href="/">
            <h2>Koosha piano Shop &#119072;</h2>
        </a>
        <%if(!user) {%>
            <text></text>
            <a class="button" name="login" href="/login">Login</a>
            <a class="button" href="/signup">SignUp</a>
            <%} else {%>
                <a class="button" name="menu" href="/newItem">Start trade </a>
                <a class="button" name="menu" href="/profile">My profile </a>
                <a class="button" href="/logout">Logout</a>
                <%}%>
    </div>
    <hr class="solid">
    <div class="header-shadow">
        <a class="button" href="/connections">items</a>
        <%if(user) {%>
            <text></text><text></text><text></text>
            <text class='right-name'> Welcome <%=user.firstName +'!' %></text>
            <%}%>

    </div>

    <% if(errorMessages.length> 0) {%>
        <% errorMessages.forEach(message=>{%>
            <div class='error'>
                <%=message%>
            </div>
            <%});%>
                <%}%>

                    <% if(successMessages.length> 0) {%>

                        <% successMessages.forEach(message=>{%>
                            <div class='success'>
                                <%=message%>
                            </div>
                            <%});%>
                                <%}%>